<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout android:layout_height="match_parent"
                android:layout_width="match_parent"
                android:paddingTop="@dimen/activity_vertical_margin"
                android:paddingBottom="@dimen/activity_vertical_margin"
                android:paddingLeft="@dimen/activity_horizontal_margin"
                android:paddingRight="@dimen/activity_horizontal_margin"

                android:background="@color/black"
                xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                xmlns:app="http://schemas.android.com/apk/lib/com.app.diceroller"
                tools:context="com.example.jakobwilbrandt.DiceRoller.MainActivity"


    >


    <TextView
        android:text="Predictability"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/predictability"
        android:textSize="35sp"
        android:layout_marginBottom="13dp"
        android:layout_above="@+id/statisticText"
        android:layout_centerHorizontal="true"
        />

    <ImageView
        android:id="@+id/timeButton"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:src="@drawable/dicestart"
        android:scaleType="fitCenter"
        android:padding="0dip"
        tools:src="@drawable/dicestart"
        android:layout_marginBottom="14dp"
        android:layout_above="@+id/predictability"
        android:layout_centerHorizontal="true"
        />

    <TextView
        android:text="Predictable"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/statisticText"
        android:textSize="25sp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="20dp"
        />

    <TextView
        android:text="Hit me!"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/numberRolled"
        android:textSize="90sp"
        android:layout_above="@+id/timeButton"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="66dp"
        />


</RelativeLayout>
